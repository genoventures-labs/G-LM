apiVersion: v1
kind: Service
metadata:
  name: {{ include "glm-api.fullname" . }}
  labels:
    {{- include "glm-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "glm-api.selectorLabels" . | nindent 4 }}
