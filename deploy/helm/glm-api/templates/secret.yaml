apiVersion: v1
kind: Secret
metadata:
  name: {{ include "glm-api.fullname" . }}-secrets
  labels:
    {{- include "glm-api.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secretEnv }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
